import { Form } from 'app/components/Form';

import * as Styles from './styles';

export const RegisterVehicles: React.FC = () => {
  return (
    <Styles.Wrapper>
      <Form />
    </Styles.Wrapper>
  );
};
